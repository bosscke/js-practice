//Create classes

class itil_v3 {
  constructor(
    //define parameters:
    lifecycle_phase,
    process,
    number_of_processes_activities
  ) {
    //define properties:
    this.lifecycle_phase = lifecycle_phase;
    this.process = process;
    this.number_of_processes_activities = number_of_processes_activities;
  }
}

export default itil_v3;
